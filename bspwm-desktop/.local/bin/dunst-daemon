#!/bin/bash

. $HOME/.cache/wal/colors.sh

bg_alpha="$background$(int-to-hex $alpha)"

dunst \
  -lb "$bg_alpha" \
  -nb "$bg_alpha" \
  -cb "$bg_alpha" \
  -lf "$foreground" \
  -nf "$foreground" \
  -cf "$foreground" \
  -cfr "$color1" \
  -frame_color $color4 \
  "$@"
